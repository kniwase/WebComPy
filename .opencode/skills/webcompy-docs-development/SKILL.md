---
name: webcompy-docs-development
description: Build and maintain the WebComPy documentation site under docs_app/. Use when authoring documentation pages, demo code, or navigation that runs through webcompy generate.
---

You are working on the WebComPy documentation site. Only modify files in `docs_app/`. Never edit `docs/` directly as it is generated output.

## Key Rules

- `docs_app/` contains the source application
- `docs/` is generated by `python -m webcompy generate --app docs_app.bootstrap:app` and is gitignored
- Use `python -m webcompy start --dev --app docs_app.bootstrap:app` for local dev server (default port 8080)
- Heavy dependencies (matplotlib, numpy) require `uv sync --all-groups`

## Related Skills

- `webcompy-component-development` — for components used in docs pages
- `webcompy-browser-development` / `webcompy-server-development` — when docs demos depend on framework or CLI internals
- `webcompy-inspect` — verifying docs pages render correctly in a real browser
- `webcompy-review` — spec-driven code review for PRs
