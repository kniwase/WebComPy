{
  "$schema": "https://opencode.ai/config.json",
  "instructions": ["AGENTS.md"],
  "provider": {
    "openai-compat": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "OpenAI Compatible",
      "options": {
        "baseURL": "{env:OPENAI_COMPAT_BASE_URL}",
        "apiKey": "{env:OPENAI_COMPAT_API_KEY}"
      },
      "models": {
        "{env:OPENAI_COMPAT_MODEL}": {
          "name": "Review Model",
          "limit": {
            "context": 128000,
            "output": 64000
          }
        }
      }
    }
  },
  "watcher": {
    "ignore": ["docs/**", ".git/**", "__pycache__/**", "*.pyc"]
  },
  "agent": {
    "review": {
      "description": "CI-only read-only code reviewer for pull requests",
      "mode": "primary",
      "permission": {
        "edit": {
          "*": "deny",
          ".tmp/*": "allow",
          ".workspace/*": "allow"
        },
        "bash": {
          "git commit*": "deny",
          "git push*": "deny"
        }
      },
      "prompt": "You are a CODE REVIEWER ONLY. Read and analyze code changes in pull requests. Identify bugs, logic errors, spec violations, and code quality issues. NEVER modify files, commit changes, or push. Always respond in English.\n\nFollow these rules for every review:\n\n1. Check previous review comments first. Do NOT repeat points that have already been raised and addressed.\n\n2. Focus ONLY on the code diff (the changed files). Do not review code that was not changed.\n\n3. Check for code quality issues, potential bugs, and suggest improvements — but only for the changed code.\n\n4. If the PR diff includes changes under `openspec/changes/archive/`, read the corresponding Change artifacts (proposal.md, design.md, tasks.md, and specs/) and verify that the implementation correctly satisfies ALL requirements defined in those specs.\n\nFormat your review using this EXACT template:\n\n```\n## Code Review: <title or brief summary>\n\n### 📋 Summary\n<2–3 sentences describing what this PR does and its scope>\n\n---\n\n### 🟢 What's Good\n- <bullet points for well-done aspects, design decisions, error handling, readability, etc.>\n\n---\n\n### 🔴 Issues\n\n#### <category name>\n\n| Severity | Issue | Recommendation |\n|----------|-------|----------------|\n| 🔴 High | <description> | <fix suggestion> |\n| 🟡 Medium | <description> | <fix suggestion> |\n\n<code blocks or additional detail if needed>\n\n---\n\n### 🟡 Warnings / Considerations\n- <trade-offs, performance concerns, future risks — things that are not bugs but worth noting>\n\n---\n\n### ✅ Verdict\n\n| Category | Count |\n|----------|-------|\n| 🔴 High | <n> |\n| 🟡 Medium | <n> |\n| 🔵 Note | <n> |\n\n<1–2 sentences justifying the verdict>\n\nREVIEW_RESULT: <approved | changes_requested>\n```\n\nRules for the template:\n- Use emoji indicators consistently: 🔴=bug/blocker, 🟡=warning/medium, 🟢=positive, 🔵=note/low\n- Use tables for issue lists and verdict counts — never use bare bullet lists for issues\n- Keep sections in this exact order — do not reorder or omit sections\n- Use code blocks with language tags for code snippets\n- Use bullet points inside 🟢 What's Good and 🟡 Warnings sections\n- The REVIEW_RESULT line MUST be the very last line of the file"
    },
    "webcompy-docs": {
      "description": "Generates and maintains the documentation site under docs_app/",
      "mode": "subagent",
      "prompt": "You are working on the WebComPy documentation site. Only modify files in docs_app/. Never edit docs/ directly as it is generated output.",
      "permission": {
        "edit": "ask"
      }
    },
    "browser-runtime": {
      "description": "Analyzes and modifies code that runs in the PyScript/browser environment (reactive, components, elements, router)",
      "mode": "subagent",
      "prompt": "You are working on browser-side WebComPy code that runs via PyScript/Emscripten. Remember: no standard library modules are available at runtime; browser APIs are accessed through js module. The reactive system must work without server-side Python."
    },
    "server-runtime": {
      "description": "Analyzes and modifies server-side code (CLI, dev server, static site generator)",
      "mode": "subagent",
      "prompt": "You are working on server-side WebComPy code (CLI tools, Starlette dev server, static site generator). This code runs in standard Python with uvicorn, aiofiles, etc."
    }
  },
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["npx", "@playwright/mcp@0.0.70", "--isolated", "--console-level=debug", "--browser", "chromium", "--headless"],
      "enabled": true
    }
  },
  "command": {
    "dev": {
      "template": "Start the WebComPy development server. Run `uv run python -m webcompy start --dev` from the project root (default port 8080). Requires Node.js/npx for Playwright MCP. The server serves the docs_app app at http://localhost:8080/. Once the server is running, use Playwright MCP tools to navigate to the URL.",
      "description": "Start dev server"
    },
    "generate-docs": {
      "template": "Generate the static documentation site from docs_app/ using `python -m webcompy generate`",
      "description": "Generate docs site"
    }
  }
}